<section>
   <div class="container mt-3">
      <div class="row">
         <div class="col-md-12">
            <a href="/admin/add-category" class="btn btn-success float-right">Add Category</a>
            <table class="table mt-2">
            <thead>
            <tr>
            <th scope="col">No</th>
            <th scope="col">Name</th>
            <th scope="col">Display Name</th>
            </tr>
            </thead>
            <tbody>
            {{#each categories}}
            <tr>
            <th scope="row">1</th>
            <td>{{this.name}}</td>
            <td>{{this.displayName}}</td>
            <td><img style="width:50px; height:50px;" src="/category-images/{{this._id}}.jpg" alt="{{this.name}}"></td>
            <td>
            <a href="/admin/edit-category/{{this._id}}" class="btn btn-primary">Edit</a>
            <a href="/admin/delete-category/{{this._id}}" class="btn btn-danger" onclick="return confirm('Are you want to delete {{this.name}} ?')">Delete</a>
            </td>
            </tr>
            {{/each}}
            </tbody>
            </table>
         </div>
      </div>
   </div>
</section>